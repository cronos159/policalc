:root{
  --bg:#0a0a0a;--surface:#111;--surface2:#161616;--surface3:#1e1e1e;--surface4:#252525;
  --border:#2a2a2a;--border2:#333;--border3:#404040;
  --text:#f0f0f0;--text2:#888;--text3:#555;--text4:#3a3a3a;
  --accent:#e8ff47;--accent2:#c8df20;--accent-dim:#373d10;
  --blue:#4f8ef7;--green:#3ecf8e;--red:#f87171;--amber:#fbbf24;
  --radius:10px;--radius-sm:6px;--radius-lg:14px;
  font-size:14px;
}
*{box-sizing:border-box;margin:0;padding:0}
body{background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;min-height:100vh;line-height:1.5;transition:filter .3s}

.login-overlay{position:fixed;inset:0;background:rgba(10,10,10,.94);backdrop-filter:blur(10px);z-index:999;display:flex;align-items:center;justify-content:center;animation:fadeIn .3s}
.login-card{background:var(--surface);border:1px solid var(--border);border-radius:16px;padding:36px;width:400px;text-align:center;box-shadow:0 20px 60px rgba(0,0,0,.5)}
.login-logo{font-size:28px;font-weight:700;margin-bottom:6px;letter-spacing:-1px}
.login-logo span{color:var(--accent)}
.login-sub{color:var(--text3);font-size:11px;letter-spacing:1.5px;text-transform:uppercase;margin-bottom:28px}
.login-card input{margin-bottom:14px;text-align:center;font-size:14px;padding:11px;width:100%;background:var(--surface2);border:1px solid var(--border2);border-radius:var(--radius-sm);color:var(--text);outline:none;transition:all .2s}
.login-card input:focus{border-color:var(--accent);box-shadow:0 0 0 3px rgba(232,255,71,.08)}
.login-card input::placeholder{color:var(--text4)}
.login-msg{font-size:12px;color:var(--text3);margin-top:14px}
.login-msg.err{color:var(--red)}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}

.shell{display:grid;grid-template-columns:230px 1fr;min-height:100vh;animation:slideIn .4s}
@keyframes slideIn{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:translateY(0)}}
.sidebar{background:var(--surface);border-right:1px solid var(--border);display:flex;flex-direction:column}
.main{padding:32px 36px;overflow-y:auto;min-width:0}

.brand{padding:22px 20px;border-bottom:1px solid var(--border)}
.brand-mark{font-size:20px;font-weight:700;letter-spacing:-0.8px}
.brand-mark span{color:var(--accent)}
.brand-sub{font-size:10px;color:var(--text3);margin-top:3px;text-transform:uppercase;letter-spacing:1.2px}
.nav{padding:12px 8px;flex:1}
.nav-item{display:flex;align-items:center;gap:11px;padding:10px 12px;border-radius:var(--radius-sm);cursor:pointer;color:var(--text2);font-size:13px;transition:all .2s;margin-bottom:2px;position:relative}
.nav-item:hover{background:var(--surface3);color:var(--text);transform:translateX(2px)}
.nav-item.active{background:var(--surface3);color:var(--accent);font-weight:500}
.nav-icon{width:16px;height:16px;flex-shrink:0;opacity:.8}
.sidebar-footer{padding:14px 16px;border-top:1px solid var(--border);font-size:10px;color:var(--text3);display:flex;flex-direction:column;gap:8px}
.status-row{display:flex;justify-content:space-between;align-items:center;font-size:10px}

.lock-btn{background:transparent;border:1px solid var(--border2);color:var(--text2);padding:6px 10px;border-radius:var(--radius-sm);font-size:11px;cursor:pointer;display:flex;align-items:center;gap:5px;justify-content:center;transition:all .2s;font-family:inherit}
.lock-btn:hover{border-color:var(--accent);color:var(--accent)}

.badge-count{background:var(--red);color:#fff;border-radius:10px;padding:2px 6px;font-size:10px;font-weight:600;margin-left:auto}

.page{display:none}
.page.active{display:block;animation:fadePage .3s}
@keyframes fadePage{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:translateY(0)}}
.page-head{display:flex;justify-content:space-between;align-items:flex-end;margin-bottom:28px}
.page-title{font-size:24px;font-weight:600;letter-spacing:-0.6px}
.page-sub{color:var(--text2);font-size:13px;margin-top:2px}

.card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:20px;transition:border .2s;margin-bottom:16px}
.card:hover{border-color:var(--border2)}
.card-title{font-size:11px;text-transform:uppercase;letter-spacing:1px;color:var(--text3);margin-bottom:14px;font-weight:600;display:flex;justify-content:space-between;align-items:center}

.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:16px}
.grid-3{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}
.grid-4{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}

.metric-card{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius);padding:14px 18px;transition:all .25s}
.metric-card:hover{transform:translateY(-2px);border-color:var(--border3)}
.metric-label{font-size:10px;color:var(--text3);text-transform:uppercase;letter-spacing:.8px;margin-bottom:4px}
.metric-val{font-size:24px;font-weight:600;letter-spacing:-0.8px;line-height:1.1;transition:color .3s}
.metric-val.positive{color:var(--green)}
.metric-val.negative{color:var(--red)}
.metric-val.accent{color:var(--accent)}
.metric-val.changing{animation:flash .4s}
@keyframes flash{0%{transform:scale(1)}50%{transform:scale(1.04);color:var(--accent)}100%{transform:scale(1)}}
.metric-sub{font-size:11px;color:var(--text3);margin-top:5px}

label{font-size:11px;color:var(--text2);display:block;margin-bottom:5px;font-weight:500;text-transform:uppercase;letter-spacing:.5px}
input,select{width:100%;background:var(--surface2);border:1px solid var(--border2);border-radius:var(--radius-sm);padding:8px 11px;color:var(--text);font-size:13px;outline:none;transition:all .2s;font-family:inherit}
input:focus,select:focus{border-color:var(--accent);box-shadow:0 0 0 3px rgba(232,255,71,.08)}
input:disabled,select:disabled{opacity:.5;cursor:not-allowed}
input::placeholder{color:var(--text4)}
.input-group{margin-bottom:14px}
.input-inline{padding:5px 9px;font-size:12px}

.btn{display:inline-flex;align-items:center;gap:7px;padding:9px 16px;border-radius:var(--radius-sm);font-size:12px;font-weight:500;cursor:pointer;border:none;transition:all .2s;font-family:inherit}
.btn:disabled{opacity:.4;cursor:not-allowed}
.btn-accent{background:var(--accent);color:#0a0a0a}
.btn-accent:hover:not(:disabled){background:var(--accent2);transform:translateY(-1px);box-shadow:0 4px 12px rgba(232,255,71,.15)}
.btn-ghost{background:transparent;border:1px solid var(--border2);color:var(--text2)}
.btn-ghost:hover:not(:disabled){border-color:var(--border3);color:var(--text);background:var(--surface3)}
.btn-sm{padding:6px 12px;font-size:11px}
.btn-danger{background:transparent;border:1px solid #3a2020;color:var(--red)}
.btn-danger:hover:not(:disabled){background:#2a1515}

.tag{display:inline-flex;align-items:center;gap:4px;padding:3px 8px;border-radius:20px;font-size:10px;font-weight:600;letter-spacing:.3px;text-transform:uppercase}
.tag-blue{background:#1a2540;color:var(--blue)}
.tag-green{background:#0d2e20;color:var(--green)}
.tag-amber{background:#2e2010;color:var(--amber)}
.tag-purple{background:#1e1530;color:#a78bfa}
.tag-red{background:#2a1515;color:var(--red)}

.sep{height:1px;background:var(--border);margin:18px 0}

.spinner{width:12px;height:12px;border:2px solid var(--border2);border-top-color:var(--accent);border-radius:50%;animation:spin .6s linear infinite;display:inline-block}
@keyframes spin{to{transform:rotate(360deg)}}

[data-tip]{position:relative}
[data-tip]:hover::after{
  content:attr(data-tip);position:absolute;bottom:calc(100% + 6px);left:50%;transform:translateX(-50%);
  background:#000;color:var(--text);padding:6px 10px;border-radius:6px;font-size:11px;white-space:nowrap;z-index:10;
  border:1px solid var(--border2);box-shadow:0 4px 12px rgba(0,0,0,.4);pointer-events:none;animation:tipIn .15s
}
@keyframes tipIn{from{opacity:0;transform:translateX(-50%) translateY(2px)}to{opacity:1;transform:translateX(-50%) translateY(0)}}

.matrix-wrap{overflow-x:auto;border-radius:var(--radius);border:1px solid var(--border);background:var(--surface);margin-top:12px;max-height:600px;overflow-y:auto}
.matrix{border-collapse:separate;border-spacing:0;width:100%;font-size:12px}
.matrix th,.matrix td{padding:8px 10px;text-align:right;white-space:nowrap;border-bottom:1px solid var(--border);border-right:1px solid var(--border);transition:background .15s}
.matrix th{background:var(--surface2);font-size:10px;text-transform:uppercase;letter-spacing:.6px;color:var(--text3);font-weight:600;position:sticky;top:0;z-index:3}
.matrix th.idx-col,.matrix td.idx-col{text-align:left;position:sticky;left:0;background:var(--surface2);font-weight:500;color:var(--text);min-width:180px;z-index:2;border-right:1px solid var(--border2)}
.matrix td.idx-col{background:var(--surface)}
.matrix th.idx-col{z-index:4}
.matrix tr:last-child td{border-bottom:none}
.matrix .row-idx td{background:rgba(255,255,255,.012);cursor:pointer}
.matrix .row-idx td.idx-col{cursor:pointer;user-select:none}
.matrix .row-idx td.idx-col:hover{background:var(--surface3)}
.matrix .row-afec td{font-size:11px;color:var(--text2);background:rgba(255,255,255,.005)}
.matrix .row-total td{background:var(--accent) !important;color:#0a0a0a !important;font-weight:700;border-bottom:1px solid #c8df20}
.matrix .row-total td.idx-col{background:var(--accent2) !important;color:#0a0a0a !important}
.matrix .row-monto td{background:var(--surface3);font-weight:500;color:var(--text)}
.matrix td input.cell-input{background:transparent;border:1px solid transparent;padding:3px 5px;font-size:11px;text-align:right;width:75px;border-radius:4px;color:var(--text);transition:all .15s;font-family:inherit}
.matrix td input.cell-input:hover:not(:disabled){border-color:var(--border2);background:var(--surface3)}
.matrix td input.cell-input:focus{border-color:var(--accent);background:var(--surface4);box-shadow:0 0 0 2px rgba(232,255,71,.1)}
.matrix td input.cell-input:disabled{cursor:not-allowed}
.matrix td.auto{background:rgba(62,207,142,.04);position:relative}
.matrix td.auto::before{content:'';position:absolute;top:3px;right:3px;width:5px;height:5px;border-radius:50%;background:var(--green);opacity:.6}
.matrix td.overridden{background:rgba(251,191,36,.06)}
.matrix td.overridden::before{content:'';position:absolute;top:3px;right:3px;width:5px;height:5px;border-radius:50%;background:var(--amber)}
.matrix .col-total{background:var(--accent2) !important;color:#0a0a0a !important;font-weight:700;font-size:12px;position:sticky;right:0;border-left:1px solid var(--accent)}
.matrix .pct-pos{color:var(--green)}
.matrix .pct-neg{color:var(--red)}
.matrix .row-total .pct-pos,.matrix .row-total .pct-neg{color:#0a0a0a !important}
.matrix .row-collapsed .row-afec{display:none}

.expand-icon{display:inline-block;width:10px;color:var(--text3);transition:transform .2s;margin-right:6px;font-size:10px}
.expand-icon.collapsed{transform:rotate(-90deg)}

.source-row{display:flex;flex-wrap:wrap;gap:8px;margin-top:14px;font-size:11px;color:var(--text3)}
.source-chip{display:inline-flex;align-items:center;gap:5px;padding:5px 10px;background:var(--surface2);border:1px solid var(--border);border-radius:20px;transition:all .2s}
.source-chip.ok{border-color:#1a4030;color:var(--green)}
.source-chip.manual{border-color:#3a2a10;color:var(--amber)}
.source-chip.err{border-color:#3a2020;color:var(--red)}

.hist-card{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius);padding:16px 18px;margin-bottom:10px;transition:all .2s}
.hist-card:hover{border-color:var(--border3);transform:translateX(2px)}
.hist-head{display:flex;justify-content:space-between;align-items:flex-start}
.hist-meta{font-size:11px;color:var(--text3);margin-top:4px}

.formula-row{display:grid;grid-template-columns:1fr 90px 36px;gap:8px;align-items:center;margin-bottom:8px}
.formula-sum{display:flex;justify-content:space-between;align-items:center;padding:10px 14px;background:var(--surface2);border-radius:var(--radius-sm);margin-top:12px;font-size:13px}

.toast{position:fixed;bottom:24px;right:24px;background:var(--surface);border:1px solid var(--border2);border-radius:var(--radius);padding:12px 18px;font-size:13px;display:flex;align-items:center;gap:10px;z-index:100;box-shadow:0 8px 24px rgba(0,0,0,.4);animation:toastIn .3s;max-width:340px}
.toast.success{border-color:#1a4030}
.toast.error{border-color:#3a2020}
.toast.warn{border-color:#3a2a10}
@keyframes toastIn{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}

.modal-overlay{position:fixed;inset:0;background:rgba(0,0,0,.6);backdrop-filter:blur(4px);z-index:200;display:flex;align-items:center;justify-content:center;animation:fadeIn .2s}
.modal{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-lg);padding:24px;width:380px;animation:slideIn .25s}
.modal-title{font-size:16px;font-weight:600;margin-bottom:6px}
.modal-text{color:var(--text2);font-size:13px;margin-bottom:18px}
.modal-actions{display:flex;gap:8px;justify-content:flex-end}

.alerta-item{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius);padding:14px 16px;margin-bottom:8px;transition:all .2s;cursor:pointer}
.alerta-item.no-leida{border-left:3px solid var(--accent);background:rgba(232,255,71,.03)}
.alerta-item:hover{border-color:var(--border3)}
.alerta-title{font-size:13px;font-weight:500;margin-bottom:4px}
.alerta-msg{font-size:12px;color:var(--text2);margin-bottom:6px}
.alerta-footer{display:flex;justify-content:space-between;align-items:center;font-size:10px;color:var(--text3)}

::-webkit-scrollbar{width:6px;height:6px}
::-webkit-scrollbar-track{background:transparent}
::-webkit-scrollbar-thumb{background:var(--border3);border-radius:3px}
::-webkit-scrollbar-thumb:hover{background:#555}

@media(max-width:768px){
  .shell{grid-template-columns:1fr}
  .sidebar{display:none}
  .main{padding:20px}
  .grid-2,.grid-3,.grid-4{grid-template-columns:1fr}
  .page-head{flex-direction:column;align-items:flex-start;gap:12px}
}